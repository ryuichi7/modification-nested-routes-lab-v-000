<%= form_for @song  do |f| %>
	<%= f.hidden_field :artist_id %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :artist_name %>
  <%= artist_select(f, @artist) %>
  <br />

  <%= f.submit %>
<% end %>
